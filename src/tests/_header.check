#include "s21_decimal.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#include <limits.h>

#define S21_EPS 1e-06
#ifndef s21_max
#define s21_max(a, b) ((a) > (b) ? (a) : (b))
#endif

//checkmk clean_mode=1 test_convert.check >test.c

